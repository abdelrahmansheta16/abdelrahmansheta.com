# Python 3.12. Offline eval tooling only — never installed in CI, never imported by the app.
#
#   python3.12 -m venv .venv && . .venv/bin/activate
#   pip install -r scripts/eval/requirements.txt
#
# torch is the CPU wheel: the dialect models are small and a GPU buys nothing here.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.9.1
transformers==4.62.0
camel-tools==1.5.6
jiwer==4.0.0
