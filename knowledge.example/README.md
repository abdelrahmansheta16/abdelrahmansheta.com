# knowledge.example

A complete FAKE corpus for a fake person, "Nour Example". Every company, number, link and policy string
here is invented. It exists so that `KNOWLEDGE_DIR=knowledge.example pnpm compile:corpus` works in CI and
for a contributor who has no access to the private `portfolio-corpus` repo. Never put anything real here.
